# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  update-tracking-details-service:
    build: .
    ports:
      - "80:80"
    container_name: update-tracking-details-service
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge