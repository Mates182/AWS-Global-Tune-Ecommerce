# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  get-tracking-details-by-id-service:
    build: .
    ports:
      - "80:80"
    container_name: get-tracking-details-by-id-service
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge