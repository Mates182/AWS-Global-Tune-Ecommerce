# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  # get-shipping-details-by-id API Container
  get-shipping-details-by-id:
    build: .
    ports:
      - "80:80"
    container_name: get-shipping-details-by-id
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge