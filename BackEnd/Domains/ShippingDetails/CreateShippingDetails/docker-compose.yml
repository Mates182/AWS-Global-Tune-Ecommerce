# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  # create-shipping-details API Container
  create-shipping-details:
    build: .
    ports:
      - "80:80"
    container_name: create-shipping-details
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge