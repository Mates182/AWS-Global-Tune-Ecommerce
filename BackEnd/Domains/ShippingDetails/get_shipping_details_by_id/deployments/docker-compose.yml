# auto-generated by ginshot
# Uncomment if needed
# version: '3.8'

services:
  shipping_details-db:
    build:
      context: ../../database/shipping_details
      dockerfile: Dockerfile
    container_name: shipping_details-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    networks:
      - app-network

  # get_shipping_details_by_id API Container
  get_shipping_details_by_id:
    build: ..
    ports:
      - "80:80"
    container_name: get_shipping_details_by_id
    environment:
      - GIN_MODE=release
      - shipping_details_URI=mongodb://user:password@shipping_details-db:27017
    volumes:
      - .:/app
    networks:
      - app-network
    restart: unless-stopped
    depends_on:
      - shipping_details-db

networks:
  app-network:
    driver: bridge